# Copy to .env.local. Leave blank to run the wired screens against in-app seed (fixture mode).

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Optional alias used by some Expo + Supabase samples
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Force fixture mode even if a Supabase URL is set
# EXPO_PUBLIC_USE_FIXTURES=1
